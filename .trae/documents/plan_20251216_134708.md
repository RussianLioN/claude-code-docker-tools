# 📋 Детальный план восстановления Claude Code в Alpine Linux

## 🚨 Анализ проблемы

На основе официальной документации Claude Code и текущего состояния:

1. **Claude Code v2.0.69** требует:
   - Node.js 18+ (для npm установки)
   - Для Alpine Linux: `libgcc libstdc++ ripgrep`
   - Нужно установить `USE_BUILTIN_RIPGREP=0`

2. **Аутентификация:**
   - Claude Code требует OAuth через Claude Console или Claude.ai подписку
   - API ключ устанавливается через `export ANTHROPIC_API_KEY="..."`
   - В контейнерах требуется ручная аутентификация

3. **Проблема Z.AI:** Мы используем подмену `ANTHROPIC_BASE_URL` на Z.AI, но Claude Code v2.0.69 может игнорировать эту настройку при отсутствии валидного токена Claude Console

## 🛠️ Пошаговый план восстановления

### Этап 1: Подготовка Alpine Linux контейнера

**Цель:** Установить все необходимые зависимости для Claude Code

```bash
# Установка зависимостей для Alpine
docker exec -it <container> apk add --no-cache libgcc libstdc++ ripgrep nodejs npm

# Установка переменной окружения
export USE_BUILTIN_RIPGREP=0
```

**Критерий успеха:** `node --version` показывает 18+ и `ripgrep --version` работает

### Этап 2: Диагностика текущего состояния

**Цель:** Определить причину автоматического выхода из shell

```bash
# Проверка файловой системы
docker exec -it <container> ls -la /root/
docker exec -it <container> ls -la /root/.claude
docker exec -it <container> ls -la /root/.claude-config

# Проверка переменных окружения
docker exec -it <container> env | grep ANTHROPIC
docker exec -it <container> env | grep CLAUDE

# Проверка конфигурационных файлов
docker exec -it <container> cat /root/.claude/settings.json 2>/dev/null || echo "No settings.json"
docker exec -it <container> cat /root/.claude-config/config.json 2>/dev/null || echo "No config.json"
```

**Критерий успеха:** Определено местоположение конфигурационных файлов и переменных окружения

### Этап 3: Правильная настройка Z.AI интеграции

**Цель:** Настроить Claude Code для работы с Z.AI API

```bash
# Создание правильной структуры конфигурации
mkdir -p /root/.claude
cat > /root/.claude/settings.json << 'EOF'
{
  "ANTHROPIC_AUTH_TOKEN": "5190eb846b5b4d74b84ecda6c9947762.cNNOPku5biYnw8yD",
  "ANTHROPIC_BASE_URL": "https://api.z.ai/api/anthropic",
  "ANTHROPIC_DEFAULT_HAIKU_MODEL": "glm-4.5-air",
  "ANTHROPIC_DEFAULT_OPUS_MODEL": "glm-4.6",
  "ANTHROPIC_DEFAULT_SONNET_MODEL": "glm-4.6",
  "ANTHROPIC_MODEL": "glm-4.6",
  "alwaysThinkingEnabled": true,
  "includeCoAuthoredBy": false,
  "permissions": {
    "ask": [],
    "defaultMode": "default",
    "deny": []
  }
}
EOF

# Установка переменных окружения
export ANTHROPIC_API_KEY="5190eb846b5b4d74b84ecda6c9947762.cNNOPku5biYnw8yD"
export ANTHROPIC_BASE_URL="https://api.z.ai/api/anthropic"
export USE_BUILTIN_RIPGREP=0
```

**Критерий успеха:** Конфигурационный файл создан и переменные окружения установлены

### Этап 4: Тестирование подключения к Z.AI

**Цель:** Проверить работоспособность подключения к Z.AI API

```bash
# Тест API с помощью curl
curl -H "Authorization: Bearer 5190eb846b5b4d74b84ecda6c9947762.cNNOPku5biYnw8yD" \
     -H "Content-Type: application/json" \
     https://api.z.ai/api/anthropic/v1/models

# Тест через Node.js скрипт
node -e "
const anthropic = require('@anthropic-ai/sdk');
const client = new anthropic({
  apiKey: '5190eb846b5b4d74b84ecda6c9947762.cNNOPku5biYnw8yD',
  baseURL: 'https://api.z.ai/api/anthropic'
});

client.messages.create({
  model: 'glm-4.6',
  max_tokens: 100,
  messages: [{role: 'user', content: 'Hello'}]
}).then(response => {
  console.log('Success:', response);
}).catch(error => {
  console.error('Error:', error.message);
});
"
```

**Критерий успеха:** API возвращает список моделей или успешный ответ на тестовый запрос

### Этап 5: Запуск Claude Code с отладкой

**Цель:** Запустить Claude Code с расширенной отладкой для диагностики

```bash
# Запуск с отладочным выводом
export CLAUDE_DEBUG=1
export ANTHROPIC_LOG=debug
claude --test-auth

# Альтернативно, запуск в режиме пропуска разрешений
claude --dangerously-skip-permissions
```

**Критерий успеха:** Claude Code запускается без запроса авторизации Anthropic

### Этап 6: Проверка сохранения сессии

**Цель:** Убедиться, что сессия GLM правильно сохраняется

```bash
# Проверка содержимого директории конфигурации
ls -la /root/.claude/
ls -la /root/.claude-config/

# Проверка файлов сессии
find /root/.claude* -name "*.json" -exec echo "=== {} ===" \; -exec cat {} \;

# Проверка прав доступа
stat /root/.claude/settings.json
stat /root/.claude-config/config.json
```

**Критерий успеха:** Файлы конфигурации имеют правильные права доступа и содержат актуальные настройки

## 🔧 Команды для быстрой диагностики

### Базовая проверка контейнера

```bash
# Проверка запущенных контейнеров
docker ps

# Проверка логов контейнера
docker logs <container_name>

# Вход в контейнер для ручной диагностики
docker exec -it <container_name> /bin/bash
```

### Проверка сетевой связности

```bash
# Проверка доступности Z.AI
curl -I https://api.z.ai

# Проверка DNS разрешения
nslookup api.z.ai

# Проверка сетевых интерфейсов в контейнере
ip addr show
```

### Проверка процессов и ресурсов

```bash
# Проверка запущенных процессов
ps aux | grep claude

# Проверка использования памяти
free -h

# Проверка дискового пространства
df -h
```

## 📊 Критерии успешной реализации

1. **Alpine Linux готов:** Все зависимости установлены, Node.js 18+ работает
2. **Конфигурация применена:** Файлы settings.json и config.json созданы с правильным содержимым
3. **Переменные окружения установлены:** ANTHROPIC_API_KEY и ANTHROPIC_BASE_URL экспортированы
4. **API подключение работает:** Тестовые запросы к Z.AI API возвращают успешный ответ
5. **Claude Code запускается:** Без запроса авторизации Anthropic
6. **Сессия сохраняется:** Конфигурация и токены сохраняются между запусками

## ⚠️ Известные проблемы и решения

1. **Автоматический выход из shell:** Использовать `--dangerously-skip-permissions` флаг
2. **Проблемы с ripgrep:** Установить `USE_BUILTIN_RIPGREP=0` для Alpine
3. **Сетевые ограничения:** Проверить firewall и DNS настройки в контейнере
4. **Права доступа:** Убедиться, что файлы конфигурации доступны для чтения

## 🎯 Следующие шаги

После успешного выполнения всех этапов:

1. Протестировать полный цикл работы с GLM-4.6
2. Добавить автоматические тесты для проверки интеграции
3. Документировать процесс для других разработчиков
4. Рассмотреть возможность использования Claude Console вместо Z.AI для официальной поддержки
