# Simple Claude Test Container
FROM node:22-alpine

RUN addgroup -g 1001 -S claude && \
    adduser -S claude -u 1001 -G claude && \
    apk add --no-cache bash curl && \
    npm install -g @anthropic-ai/claude-code@latest

USER claude
WORKDIR /home/claude

ENTRYPOINT ["claude"]
