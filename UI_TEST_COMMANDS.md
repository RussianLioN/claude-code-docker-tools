# 🎯 НЕПОСРЕДСТВЕННОЕ UI-ТЕСТИРОВАНИЕ

## 🚀 Начните прямо сейчас (5 команд)

### 1. Загрузите AI Assistant

```bash
source ./ai-assistant.zsh
```

### 2. Проверьте справку

```bash
ai-mode help
```

### 3. Выполните команду в контейнере

```bash
gexec 'echo "🐳 Container working!"'
```

### 4. Проверьте Docker контейнеры

```bash
gexec 'ls -la *.md' | head -5
```

### 5. Проверьте эфемерную очистку

```bash
docker ps -aq --filter 'name=claude*'  # Должно быть 0
```

---

## 🤖 Тестирование AI функций

### Gemini (требует интернет)

```bash
gemini --help
```

### Claude (требует интернет)

```bash
claude --help
```

### AI коммит (если есть изменения в git)

```bash
# Сначала создайте тестовое изменение
echo "test" >> test.md && git add test.md

# AI коммит через Gemini
aic "Add test file"

# AI коммит через Claude
cic "Add test file via Claude"
```

---

## 🔧 Полный тестовый сценарий

```bash
# 1. Загрузка
source ./ai-assistant.zsh

# 2. Проверка системы
ai-mode help
ensure_docker_running
ensure_ssh_loaded

# 3. Тест контейнера
gexec 'echo "Test successful"'
gexec 'pwd && ls -la'

# 4. AI тесты
gemini --help  # или
claude --help  # или

# 5. Проверка очистки
docker ps -aq --filter 'name=claude*'  # Должно быть 0
```

---

## ✅ Ожидаемые результаты

1. **Загрузка**: Приветственное сообщение без ошибок
2. **ai-mode help**: 18 строк справки
3. **gexec**: Вывод команды внутри контейнера
4. **Docker cleanup**: 0 orphaned контейнеров
5. **AI help**: Справка от Gemini/Claude (может потребоваться загрузка контейнера)

---

## 🚨 Если что-то не работает

### Docker не запускается

```bash
# Попробуйте запустить вручную
docker info

# Или Colima (для macOS)
colima start
```

### Контейнеры не загружаются

```bash
# Проверьте образ
docker images | grep claude

# Загрузите вручную (при необходимости)
docker pull ghcr.io/anthropics/claude-code-tools:latest
```

### SSH агент не работает

```bash
# Проверьте агент
ssh-add -l

# Добавьте ключи
ssh-add ~/.ssh/id_rsa
```

---

## 📊 Репортинг результатов

Сообщите:

- ✅ Какие команды сработали
- ❌ Какие команды не сработали
- ⏱️ Время запуска контейнеров
- 🔍 Любые ошибки или предупреждения

**Готово к тестированию!** 🚀
