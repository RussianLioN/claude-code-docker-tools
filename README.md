# Claude Code + Gemini Dual AI Assistant: Enterprise Environment

![Version](https://img.shields.io/badge/version-2.1.0-blue.svg?style=flat-square)
![Architecture](https://img.shields.io/badge/architecture-Dual%20AI%20%2B%20SSH%20Forwarding-purple.svg?style=flat-square)
![Security](https://img.shields.io/badge/security-Zero%20Trust-green.svg?style=flat-square)
![Status](https://img.shields.io/badge/status-production--ready-success.svg?style=flat-square)

**Enterprise-grade dual AI environment for Claude Code + Gemini development on macOS.**

---

## 📑 Оглавление (Table of Contents)

1.  [Введение и Философия Проекта](#1-введение-и-философия-проекта)
2.  [Архитектура и Технические Решения](#2-архитектура-и-технические-решения)
    *   [Dual AI Architecture](#dual-ai-architecture)
    *   [Изоляция через Docker (Alpine Linux)](#изоляция-через-docker-alpine-linux)
    *   [Паттерн Sync In / Sync Out](#паттерн-sync-in--sync-out-решение-проблемы-virtiofs)
    *   [Сетевая Безопасность (SSH Agent Forwarding)](#сетевая-безопасность-ssh-agent-forwarding)
    *   [In-Memory Context (AIC/CIC)](#in-memory-context-aiccic-architecture)
3.  [Quick Start (Шпаргалка)](#3-quick-start-шпаргалка)
4.  [Установка и Развертывание](#4-установка-и-развертывание)
5.  [Конфигурация и Управление Режимами](#5-конфигурация-и-управление-режимами)
6.  [Управление Секретами и Безопасность](#6-управление-секретами-и-безопасность)
7.  [Рабочие Процессы (Workflows)](#7-рабочие-процессы-workflows)
    *   [Gemini Режим](#gemini-режим)
    *   [Claude Code Режим](#claude-code-режим)
    *   [AI-Автоматизация Коммитов](#ai-автоматизация-коммитов)
    *   [Системный Режим (Gexec)](#системный-режим-gexec)
8.  [Сравнение AI Режимов](#8-сравнение-ai-режимов)
9.  [Troubleshooting и FAQ](#9-troubleshooting-и-faq)
10. [Приложение А: Конфигурация Claude API](#10-приложение-а-конфигурация-claude-api)

---

## 1. Введение и Философия Проекта

В современной экосистеме разработки (особенно на macOS) управление зависимостями CLI-инструментов становится критической проблемой ("Dependency Hell"). С появлением множественных AI ассистентов (Gemini, Claude Code) возникает необходимость в унифицированной среде.

### Наше Решение: Dual AI Infrastructure as Code (IaC)
Мы создали единую Docker-контейнеризованную среду, поддерживающую как Google Gemini CLI, так и Claude Code, с прозрачной интеграцией в хост-систему (macOS).

**Ключевые возможности:**
*   🚀 **Dual AI Support:** Единая среда для Gemini и Claude Code.
*   🔒 **Secure by Design:** Секреты живут только в оперативной памяти контейнера.
*   🧠 **Context Aware:** Система понимает, в каком Git-проекте и AI режиме вы находитесь.
*   🔄 **Self-Healing & Auto-Update:** Автоматические обновления обоих AI инструментов.
*   ⚡ **Mode Switching:** Мгновенное переключение между AI ассистентами.

---

## 🇨🇳 GLM-4.6 Support (via Z.AI)

Проект поддерживает работу с моделью **GLM-4.6** через API провайдера Z.AI.
Это реализовано через параллельный контейнер, который использует инфраструктуру Claude Code, но перенаправляет запросы.

### Настройка GLM
1.  Получите API Key на [Z.AI](https://docs.z.ai).
2.  Сохраните ключ в secrets:
    ```bash
    mkdir -p ~/.docker-ai-config/global_state/secrets
    echo "your_zai_api_key" > ~/.docker-ai-config/global_state/secrets/zai_key
    ```
3.  Запустите GLM:
    ```bash
    glm
    # Или через ai-mode
    ai-mode glm
    ```

### Параллельная работа
Вы можете запускать `claude` и `glm` одновременно в разных терминалах. Они используют разные контейнеры и независимые директории состояния (`.claude_config` vs `.glm_config`), поэтому история чатов не пересекается.

## 2. Архитектура и Технические Решения

### Dual AI Architecture

```
┌─────────────────────────────────────────────────────────┐
│                 macOS Host System                      │
├─────────────────────────────────────────────────────────┤
│  ai-assistant.zsh (Unified Wrapper)                    │
│    ├─ gemini() → Docker (Gemini Mode)                   │
│    ├─ claude() → Docker (Claude Mode)                   │
│    ├─ aic() / cic() (AI Commits)                        │
│    └─ gexec() (System Commands)                         │
└─────────────────────────────────────────────────────────┘
                           │
                    Docker Network
                           │
┌─────────────────────────────────────────────────────────┐
│            claude-code-tools Container                  │
├─────────────────────────────────────────────────────────┤
│  entrypoint.sh (Mode Detection)                         │
│    ├─ AI_MODE=claude → Claude CLI                       │
│    ├─ AI_MODE=glm    → GLM-4.6 (Z.AI)                   │
│    └─ AI_MODE=gemini → Gemini CLI                       │
│                                                         │
│  /workspace (Mounted Volume)                            │
│  /root/.ai (State Sync)                                 │
│  SSH Agent Forwarding                                   │
└─────────────────────────────────────────────────────────┘
```

### Изоляция через Docker (Alpine Linux)
Базовый образ `node:22-alpine` модифицирован для поддержки обоих AI инструментов:
- **Gemini CLI**: @google/gemini-cli
- **Claude Code Tools**: @anthropic-ai/claude-cli
- **Системные пакеты**: bash, coreutils, git, openssh, github-cli, curl, wget

### Паттерн Sync In / Sync Out (Решение проблемы VirtioFS)
Адаптированный алгоритм **State Synchronization** для dual-режима:
1.  **Boot Phase (Sync In):** Копирование конфигураций из глобального хранилища `~/.docker-ai-config/global_state` в контейнер.
2.  **Runtime Phase:** Docker монтирует `.ai-state/` с конфигурациями обоих AI.
3.  **Shutdown Phase (Sync Out):** Сохранение обновленных конфигураций обратно.

### Adaptive Workspace (Smart Mounting)
Система автоматически определяет контекст запуска:
- **Git Root:** Монтируется в `/workspace/<project_name>`.
- **Subdirectory:** Корень Git монтируется в `/workspace`, скрывая имя родительской папки.
- **No Git:** Текущая папка монтируется в `/workspace/<folder_name>`.

### Сетевая Безопасность (SSH Agent Forwarding)
Пробрасывание Unix-сокета SSH агента: `-v /run/host-services/ssh-auth.sock:/run/host-services/ssh-auth.sock`.
**Sanitization** SSH-конфига для совместимости с Linux средой контейнера.

### In-Memory Context (AIC/CIC Architecture)
**Gemini AI Commit (aic)** и **Claude AI Commit (cic)**:
- Контекст (Git история + Diff) передается напрямую через аргументы командной строки
- Минимизация файловых операций для безопасности
- Адаптированные промпты под каждый AI ассистент

---

## 3. Quick Start (Шпаргалка)

### Основные Команды

| Команда | AI Режим | Назначение | Сценарий использования |
| :--- | :--- | :--- | :--- |
| **`gemini`** | Gemini | **Интерактивный Чат** | Генерация кода, рефакторинг |
| **`claude`** | Claude | **Интерактивный Чат** | Анализ кода, дебаггинг |
| **`aic`** | Gemini | **AI Commit** | Коммиты в стиле DevOps |
| **`cic`** | Claude | **AI Commit** | Коммиты в стиле Software Engineer |
| **`gexec`** | - | **God Mode** | Выполнение команд в контейнере |
| **`ai-mode`** | - | **Switcher** | Переключение режимов |

### Сценарий "Типичный цикл разработки"

```bash
# 1. Работа с Gemini
cd ~/my-project
gemini
> "Проанализируй @server.js"
> /exit

# 2. Автоматический Gemini коммит
# 🤖 Gemini анализирует изменения...
# 📝 Предложенный коммит: feat(server): optimize logic
# 🚀 Выполнить commit? [y/N]: y

# 3. Переключение на Claude
ai-mode claude
claude
> "Найди баги в этом коде..."
> /exit

# 4. Автоматический Claude коммит
# 🤖 Claude анализирует изменения...
# 📝 Предложенный коммит (Claude): fix(server): resolve null pointer exception
```

---

## 4. Установка и Развертывание

### Требования
*   macOS (Apple Silicon / Intel).
*   Docker Desktop 4.0+.
*   SSH ключи (Ed25519), добавленные в агент.
*   **Claude API Key** (для Claude режима).

### Шаг 1: Клонирование репозитория
```bash
mkdir -p ~/tools && cd ~/tools
git clone git@github.com:RussianLioN/claude-code-docker-tools.git .
```

### Шаг 2: Инсталляция
```bash
./install.sh
source ~/.zshrc
```
Инсталлятор автоматически соберет образ с поддержкой обоих AI инструментов.

### Шаг 3: Настройка Claude API
```bash
# Редактирование файла окружения
nano ~/.docker-ai-config/env

# Раскомментируйте и установите ваш Claude API ключ
export CLAUDE_API_KEY="sk-ant-api03-..."
```

---

## 5. Конфигурация и Управление Режимами

### Автоматическое Обновление
Скрипт `ai-assistant.zsh` автоматически проверяет обновления для обоих инструментов при интерактивном запуске.

### Переключение Режимов
```bash
ai-mode gemini    # Переключение в Gemini режим
ai-mode claude    # Переключение в Claude режим
ai-mode           # Показ текущего режима
```

### Файлы Конфигурации

**Новый unified путь:** `~/.docker-ai-config/`
*   **`settings.json`**: Настройки Gemini модели.
*   **`claude_config.json`**: Настройки Claude модели.
*   **`env`**: Переменные окружения (API ключи).
*   **`google_accounts.json`**: Google аккаунты для Gemini.

**⚠️ Миграция из старой системы:**

Если у вас установлена старая версия (`gemini-docker-setup`), система автоматически:

1. Обнаружит credentials в `~/.docker-gemini-config/`
2. Предложит автоматическую миграцию
3. Создаст backup перед миграцией

```bash
# Ручная миграция при необходимости
./scripts/migrate-credentials.sh

# Управление credentials
./scripts/credential-manager.sh status
./scripts/credential-manager.sh migrate
```

**Подробнее:** [CREDENTIAL_MIGRATION_GUIDE.md](./CREDENTIAL_MIGRATION_GUIDE.md)

---

## 6. Управление Секретами и Безопасность

### Принцип Zero Trust
Секреты никогда не покидают ваш диск:
```gitignore
.ai-state/              # Временное хранилище
.docker-ai-config/      # Глобальные конфигурации
google_accounts.json    # Gemini OAuth токены
claude_config.json      # Claude конфигурация
.env                    # API ключи
```

**Авто-защита:** Функции `aic` и `cic` автоматически добавляют `.ai-state/` в `.gitignore`.

---

## 7. Рабочие Процессы (Workflows)

### Gemini Режим
Запускает контейнер с Gemini CLI и контекстом проекта.
*   **Сильные стороны:** DevOps подход, системное мышление, автоматизация.
*   **Идеально для:** Infrastructure as Code, CI/CD, скриптов, архитектуры.

### Claude Code Режим
Запускает контейнер с Claude Code и контекстом проекта.
*   **Сильные стороны:** Глубокий анализ кода, дебаггинг, рефакторинг.
*   **Идеально для:** Code review, поиска багов, оптимизации алгоритмов.

### AI-Автоматизация Коммитов
**Gemini (aic):** Фокус на DevOps стиле, semantic commits.
**Claude (cic):** Фокус на Software Engineering стиле, детальные описания.

### Системный Режим (Gexec)
Единая точка входа для выполнения команд через настроенное окружение Docker.

---

## 8. Сравнение AI Режимов

| Аспект | Gemini | Claude Code |
| :--- | :--- | :--- |
| **Стиль** | DevOps Engineer | Senior Software Engineer |
| **Сильные стороны** | Системы, автоматизация, CI/CD | Код, алгоритмы, архитектура |
| **Коммиты** | Конвенционные, semantic | Детальные, описательные |
| **Контекст** | Инфраструктура, процессы | Код, паттерны, лучшие практики |
| **API** | OAuth Google | API Key Anthropic |

---

## 9. Troubleshooting и FAQ

### Q: Ошибка "CLAUDE_API_KEY not found"
**Причина:** Не установлен API ключ для Claude.
**Решение:** Отредактируйте `~/.docker-ai-config/env` и добавьте ключ.

### Q: Как переключиться между AI режимами?
**Решение:** Используйте команду `ai-mode <gemini|claude>`.

### Q: Ошибка SSH подключения в Claude режиме
**Причина:** Ключ отсутствует в SSH агенте.
**Решение:** `ssh-add --apple-use-keychain ~/.ssh/id_ed25519`

### Q: В чем разница между aic и cic?
**aic** - Gemini AI Commit, **cic** - Claude AI Commit. Разные стили коммитов.

---

## 10. Приложение А: Конфигурация Claude API

### Получение API ключа
1. Перейдите на [Anthropic Console](https://console.anthropic.com/)
2. Создайте новый API ключ
3. Добавьте ключ в `~/.docker-ai-config/env`

### Пример конфигурации
```bash
# ~/.docker-ai-config/env
export CLAUDE_API_KEY="sk-ant-api03-..."
export CLAUDE_MODEL="claude-3-5-sonnet-20241022"
export CLAUDE_MAX_TOKENS=4096
export AI_CURRENT_MODE="gemini"  # или "claude"
```

### Модели Claude
- **claude-3-5-sonnet-20241022**: Основная модель (рекомендуется)
- **claude-3-opus-20240229**: Максимальные возможности
- **claude-3-haiku-20240307**: Быстрые ответы

---
*Documentation Version 2.1.0. Ephemeral Architecture Implementation.*
