# 🚀 Исправление Claude Code в Docker контейнере

## Проблема
Claude Code в контейнере выдавал ошибки `EACCES: permission denied` при попытке создать директории в `~/.claude/session-env/` и `~/.claude/projects/`.

## Решение
Созданы два новых скрипта с правильным мапингом и правами доступа:

### 1. `claude-glm-fixed.sh` - Рекомендуемый вариант
- Использует `--user "$(id -u):$(id -g)"` для совпадения UID/GID
- Правильный volume mapping: `-v ~/.claude:/home/aiuser/.claude:rw`
- Автоматическая настройка прав при запуске
- Создает бэкап ~/.claude перед запуском

### 2. `claude-glm-root.sh` - Альтернативный вариант
- Запускается от root с полными правами
- Мапинг в `/root/.claude` вместо `/home/aiuser/.claude`
- Гарантирует полный доступ ко всем файлам

## Использование

### Вариант 1: Исправленный скрипт (рекомендуется)
```bash
cd /Users/s060874gmail.com/coding/projects/claude-code-docker-tools
./claude-glm-fixed.sh
```

### Вариант 2: Root скрипт (если первый не сработает)
```bash
cd /Users/s060874gmail.com/coding/projects/claude-code-docker-tools
./claude-glm-root.sh
```

### Вариант 3: Тестирование прав доступа
```bash
./test-permissions.sh          # Тест от aiuser
./test-root-permissions.sh     # Тест от root
```

## Ключевые изменения

1. **Правильный UID/GID**: Используется `--user "$(id -u):$(id -g)"` для совпадения с хостом
2. **Volume mapping**: `-v ~/.claude:/home/aiuser/.claude:rw` с правами на запись
3. **Автоматические права**: Скрипт автоматически устанавливает правильные права при запуске
4. **Бэкапы**: Создаются бэкапы ~/.claude перед изменениями
5. **Environment variables**: Все переменные окружения передаются в контейнер

## Экспертные мнения

- **Senior Docker Engineer**: "UID/GID mismatch - классическая проблема, решается через `--user` флаг"
- **Unix Script Expert**: "Нужно проверить права на директории и установить 755/644"
- **DevOps Engineer**: "Volume mapping с `:rw` и правильные user permissions решат проблему"
- **AI IDE Expert**: "Claude Code требует полных прав на ~/.claude для создания сессий"

## Следующие шаги

1. Протестировать `claude-glm-fixed.sh`
2. Если не работает, попробовать `claude-glm-root.sh`
3. Проверить, что Claude Code больше не запрашивает авторизацию
4. Убедиться, что все команды bash работают в контейнере

## Файлы

- `claude-glm-fixed.sh` - Основной исправленный скрипт
- `claude-glm-root.sh` - Альтернативный root скрипт
- `test-permissions.sh` - Тест прав доступа
- `test-root-permissions.sh` - Тест прав доступа от root