# 🧪 Пользовательское тестирование AI Assistant

## 🎯 Цель

Проверить работу экспертной эфемерной архитектуры в реальных сценариях использования.

## 📋 Подготовка

### 1. Откройте терминал и перейдите в проект:
```bash
cd /Users/s060874gmail.com/coding/projects/claude-code-docker-tools
```

### 2. Загрузите AI Assistant:
```bash
source ./ai-assistant.zsh
```
*Вы должны увидеть приветственное сообщение*

---

## 🚀 ТЕСТ 1: Базовая навигация

### 1.1 Проверка справки
```bash
ai-mode help
```
**Ожидаемый результат**: Справка по доступным режимам (gemini/claude)

### 1.2 Проверка статуса Docker
```bash
ensure_docker_running
```
**Ожидаемый результат**: "Docker is running!" или автоматический запуск

### 1.3 Проверка SSH агента
```bash
ensure_ssh_loaded
```
**Ожидаемый результат**: "SSH agent loaded with X keys"

---

## 🤖 ТЕСТ 2: AI ассистенты

### 2.1 Gemini AI Assistant
```bash
gemini --help
```
**Ожидаемый результат**: Справка по Gemini (может потребоваться загрузка контейнера)

### 2.2 Claude Code Assistant
```bash
claude --help
```
**Ожидаемый результат**: Справка по Claude Code (может потребоваться загрузка контейнера)

---

## 🔧 ТЕСТ 3: Выполнение в AI окружении

### 3.1 Простая команда в контейнере
```bash
gexec 'echo "Hello from AI container"'
```
**Ожидаемый результат**: "Hello from AI container"

### 3.2 Работа с файлами в проекте
```bash
gexec 'ls -la README.md'
```
**Ожидаемый результат**: Информация о файле README.md

### 3.3 Проверка окружения
```bash
gexec 'whoami && pwd && date'
```
**Ожидаемый результат**: Информация о пользователе, директории и времени

---

## 🔄 ТЕСТ 4: Конфигурация

### 4.1 Подготовка конфигурации
```bash
prepare_configuration
```
**Ожидаемый результат**: Создание .ai-state директории

### 4.2 Проверка состояния
```bash
echo $STATE_DIR
```
**Ожидаемый результат**: Путь к .ai-state директории

### 4.3 Очистка конфигурации
```bash
cleanup_configuration
```
**Ожидаемый результат**: Очистка временных файлов

---

## 💻 ТЕСТ 5: AI-powered коммиты (если есть изменения)

### 5.1 Создайте тестовый файл
```bash
echo "# Test file for AI commit" > test-ai-commit.md
```

### 5.2 Добавьте в git
```bash
git add test-ai-commit.md
```

### 5.3 Gemini AI commit
```bash
aic "Add test file for AI commit functionality validation"
```
**Ожидаемый результат**: Автоматически созданный commit message

### 5.4 Claude AI commit (если нужно еще коммитов)
```bash
# Сначала сделайте еще изменения
echo "Updated content" >> test-ai-commit.md
git add test-ai-commit.md

cic "Update test file with additional content"
```
**Ожидаемый результат**: Claude-стиль commit message

---

## 🎯 ТЕСТ 6: Реальный рабочий сценарий

### 6.1 Получите помощь от AI по коду
```bash
gexec 'cat ai-assistant.zsh | head -20' > temp-code.txt
gemini "Analyze this code and explain what it does" < temp-code.txt
```
**Ожидаемый результат**: Анализ кода от Gemini

### 6.2 Получите помощь от Claude
```bash
claude "What is the purpose of this project based on the file structure?"
```
**Ожидаемый результат**: Интерактивная сессия с Claude

---

## 🧹 ТЕСТ 7: Проверка эфемерной очистки

### 7.1 Проверьте контейнеры ДО тестов
```bash
docker ps -aq --filter 'name=claude*'
```

### 7.2 Выполните несколько команд
```bash
gexec 'echo "test 1"'
gexec 'echo "test 2"'
gexec 'echo "test 3"'
```

### 7.3 Проверьте контейнеры ПОСЛЕ тестов
```bash
docker ps -aq --filter 'name=claude*'
```
**Ожидаемый результат**: **0 контейнеров** (эфиремерная архитектура работает!)

---

## 📊 Форма обратной связи

### ✅ Успешные тесты (что сработало):
- [ ] Базовая навигация
- [ ] AI ассистенты запускаются
- [ ] Команды выполняются в контейнере
- [ ] Конфигурация синхронизируется
- [ ] AI коммиты работают
- [ ] Эфемерная очистка работает

### ❌ Проблемы (что не сработало):
- [ ] Docker не запускается
- [ ] Контейнеры не загружаются
- [ ] SSH агент не работает
- [ ] AI коммиты не создаются
- [ ] Остаются orphaned контейнеры

### 📝 Комментарии:
- Время запуска контейнера: ______ секунд
- Появились ли ошибки: ____________________
- Дополнительные замечания: __________________

---

## 🎯 Дополнительные тесты (опционально)

### Тест производительности:
```bash
# Замерьте время запуска
time gexec 'echo "performance test"'
```

### Тест с большими командами:
```bash
gexec 'find . -name "*.md" | wc -l'
```

### Тест интеграции с GitHub CLI (если установлен):
```bash
gh --version
```

---

## 🔧 Решение проблем

### Если Docker не запускается:
```bash
# Попробуйте Colima (если Docker Desktop не работает)
colima start
```

### Если контейнеры не загружаются:
```bash
# Проверьте образ
docker images | grep claude
```

### Если SSH не работает:
```bash
ssh-add -l
```

---

## ✅ Завершение тестирования

После завершения всех тестов запустите:
```bash
# Проверьте чистоту системы
docker ps -aq --filter 'name=claude*'  # Должно быть 0
docker images | grep claude           # Образ должен быть

# Очистите тестовые файлы
rm -f temp-code.txt test-ai-commit.md
```

**Сообщите результаты через форму обратной связи выше!**