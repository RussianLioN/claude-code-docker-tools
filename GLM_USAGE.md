# GLM Контейнер - Инструкция по использованию

## 🚀 Быстрый старт

### 1. Сборка контейнера (выполняется один раз)
```bash
./run-glm-workspace.sh
```

### 2. Тестовый запуск
```bash
./test-glm.sh "Ваше сообщение"
```

### 3. Интерактивный режим
```bash
./start-glm.sh
```

## 📋 Файлы и их назначение

- **`config/glm_config.json`** - Конфигурация с вашими API ключами
- **`run-glm-workspace.sh`** - Сборка и запуск контейнера
- **`start-glm.sh`** - Запуск в интерактивном режиме
- **`test-glm.sh`** - Тестовый запуск с одним сообщением
- **`workspaces/glm-workspace`** - Рабочая директория внутри контейнера

## 🔧 Конфигурация

Используется правильный URL: `https://api.z.ai/api/anthropic`

Модели:
- `glm-4.6` - основная модель
- `glm-4.5-air` - быстрая модель

## 📝 Примеры использования

### Отправить одно сообщение:
```bash
./test-glm.sh "Напиши функцию на Python для сортировки массива"
```

### Интерактивная сессия:
```bash
./start-glm.sh
```
В интерактивном режиме введите сообщение и нажмите Enter. Для выхода введите `exit`.

## 🐛 Поиск проблем

### Проверить статус контейнера:
```bash
docker ps -a | grep glm
```

### Посмотреть логи:
```bash
docker logs glm-zai-session
```

### Пересобрать образ:
```bash
docker rmi glm-zai
./run-glm-workspace.sh
```

## ✅ Результат

GLM контейнер успешно настроен и работает с API `https://api.z.ai/api/anthropic`. Контейнер использует модель `glm-4.6` и готов к работе!