# TRAE IDE Integration Guide

## 🎯 Оптимальное решение для pre-commit хуков

### Проблема
TRAE IDE автоматически выполняет pre-commit хуки при коммите, что вызывает конфликты между staged и unstaged файлами, когда хуки пытаются автоматически исправить форматирование.

### Решение: Умный pre-commit подход

#### 1. Использование кастомного хука
```bash
# Активировать умный хук вместо стандартного pre-commit
.git/hooks/pre-commit-smart
```

#### 2. Обновленная конфигурация .pre-commit-config.yaml
```yaml
# Разделение хуков на проверочные и исправляющие
repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.5.0
    hooks:
      # Только проверка при автоматическом запуске
      - id: check-yaml
        stages: [commit]
      - id: check-json
        stages: [commit]
      - id: check-added-large-files
        stages: [commit]

      # Ручное форматирование при необходимости
      - id: trailing-whitespace
        stages: [manual]
      - id: end-of-file-fixer
        stages: [manual]
```

### Workflow с TRAE

#### Способ A: Полуавтоматический (Рекомендуемый)
1. Сделайте изменения в файлах
2. Запустите форматирование: `./scripts/trae-smart-commit.sh "ваше сообщение"`
3. Форматирование и коммит выполняются автоматически

#### Способ B: Полностью ручной
1. Сделайте изменения
2. Отформатируйте: `.git/hooks/pre-commit-smart`
3. Добавьте файлы: `git add .`
4. Сделайте коммит через TRAE IDE

#### Способ C: Через SKIP переменную
```bash
# Временно пропустить форматирование
SKIP=trailing-whitespace,end-of-file-fixer git commit -m "сообщение"
```

### Преимущества подхода

✅ **Нет конфликтов**: Форматируются только staged файлы
✅ **Контроль качества**: Проверочные хуки работают автоматически
✅ **Интеграция**: Работает с любым Git клиентом, включая TRAE
✅ **Гибкость**: Можно выбрать уровень автоматизации
✅ **Безопасность**: Никакие изменения не теряются

### Быстрые команды

```bash
# Умный коммит
./scripts/trae-smart-commit.sh "fix: исправление форматирования"

# Только форматирование без коммита
.git/hooks/pre-commit-smart

# Ручное форматирование всех файлов
pre-commit run trailing-whitespace,end-of-file-fixer --all-files
```
