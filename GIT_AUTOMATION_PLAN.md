# План Автоматизации Git Процедур: Commit & Push Workflow

**📅 Дата**: 2025-12-18
**🎯 Цель**: Создание полностью автоматизированного workflow для коммита и пуша изменений
**🔍 Анализ существующей автоматизации**: Выполнен

---

## 📊 Анализ Существующей Автоматизации

### ✅ Уже реализовано

- **CI/CD Pipelines**: `.github/workflows/optimized-ci.yml`, `ci.yml`, `enterprise-ci.yml`
- **Pre-commit hooks**: `.pre-commit-config.yaml` с shellcheck, markdownlint, JSON/YAML валидацией
- **Version Management**: `scripts/version-manager.sh` с автоматическим управлением версиями
- **AI-powered коммиты**: Команды `aic` (Gemini AI Commit) и `cic` (Claude AI Commit)

### ❌ Отсутствует

- Единый автоматизированный workflow для commit + push
- Интеллектуальное формирование сообщений коммита
- Автоматическое определение типов изменений

---

## 🚀 Пошаговый План Автоматизации

### 📋 Этап 1: Анализ Изменений

#### 1.1 Определение измененных и новых файлов

```bash
# Базовые команды для анализа
git status --porcelain                    # Короткий статус
git diff --name-only                      # Измененные файлы
git ls-files --others --exclude-standard   # Новые файлы
git diff --stat                           # Статистика изменений
```

**Инструменты**:

- `git status --porcelain` - машинночитаемый формат
- `git diff --name-status` - файлы + тип изменений
- Custom parser для классификации файлов

#### 1.2 Проверка статуса репозитория

```bash
# Проверки перед коммитом
git status                                   # Общий статус
git log --oneline -5                         # Последние коммиты
git branch --show-current                    # Текущая ветка
git remote -v                                # Удаленные репозитории
```

**Контрольные точки**:

- [ ] Рабочая директория не пуста
- [ ] Текущая ветка определена
- [ ] Удаленный репозиторий доступен
- [ ] Нет конфликтов слияния

#### 1.3 Подтверждение корректности изменений

```bash
# Валидация изменений
pre-commit run --all-files                   # Pre-commit проверки
shellcheck **/*.sh                          # Проверка shell скриптов
markdownlint **/*.md                         # Проверка Markdown
yamllint **/*.yml **/*.yaml                 # Проверка YAML файлов
```

**Инструменты**:

- Интеграция с существующим `.pre-commit-config.yaml`
- Дополнительные проверки для специфичных файлов

---

### 📝 Этап 2: Подготовка к Коммиту

#### 2.1 Формирование осмысленного сообщения коммита

**Автоматическая классификация изменений**:

```bash
# Типы изменений и их паттерны
feat:     Новые функции (feat: add AI assistant)
fix:      Исправления ошибок (fix: resolve Docker build issue)
docs:     Документация (docs: update README)
style:    Форматирование кода (style: fix indentation)
refactor: Рефакторинг (refactor: optimize Docker layers)
test:     Тесты (test: add integration tests)
chore:    Обслуживание (chore: update dependencies)
```

**Алгоритм формирования сообщения**:

1. **Анализ измененных файлов** → определение типа
2. **Паттерны файлов** → контекст изменения
3. **Размер изменений** → важность (major/minor/patch)
4. **Git история** → согласованность с предыдущими коммитами

#### 2.2 Автоматическое добавление файлов

```bash
# Умное добавление файлов
git add .                                    # Все файлы
git add -u                                   # Только измененные
git add **/*.sh **/*.md                      # Конкретные паттерны
```

**Стратегии добавления**:

- **Conservative**: только измененные файлы
- **Standard**: измененные + новые файлы
- **Aggressive**: все файлы включая игнорируемые

#### 2.3 Проверка на наличие конфликтов

```bash
# Проверка конфликтов
git status                                   # Конфликты (U)
git diff --name-only --diff-filter=U         # Только конфликтующие
git merge --abort                            # Отмена слияния при проблемах
```

**Контрольные точки**:

- [ ] Нет merge конфликтов
- [ ] Все изменения staged корректно
- [ ] Сообщение коммита сформировано

---

### ✅ Этап 3: Процедура Коммита

#### 3.1 Создание локального коммита

```bash
# Выполнение коммита
git commit -m "автоматически_сформированное_сообщение"
git commit --no-verify -m "пропустить_pre-commit"  # Опционально
```

**Варианты коммита**:

- **Standard**: обычный коммит с проверками
- **Force**: force commit с --no-verify
- **Amend**: исправление последнего коммита

#### 3.2 Проверка истории коммитов

```bash
# Валидация коммита
git log --oneline -1                         # Последний коммит
git show --stat                              # Детали коммита
git diff HEAD~1 HEAD                         # Изменения в коммите
```

**Проверки качества**:

- [ ] Коммит создан успешно
- [ ] Сообщение соответствует conventional commits
- [ ] Все изменения включены в коммит

#### 3.3 Валидация изменений перед пушем

```bash
# Финальная валидация
git status                                   # Чистый статус
git log --oneline -3                         # История
git diff origin/main..HEAD                   # Отличия от remote
```

---

### 🚀 Этап 4: Процедура Пуша

#### 4.1 Определение целевой ветки

```bash
# Определение ветки
current_branch=$(git branch --show-current)
default_branch="main"                        # или "develop"
target_branch="${1:-$current_branch}"        # Параметр или текущая
```

**Стратегии выбора ветки**:

- **Current**: пуш в текущую ветку
- **Default**: пуш в основную ветку (main/develop)
- **Feature**: пуш в feature ветку с именем

#### 4.2 Отправка изменений на удаленный репозиторий

```bash
# Операции пуша
git push origin $target_branch               # Стандартный пуш
git push -u origin $target_branch            # Set upstream
git push --force-with-lease                  # Безопасный force push
```

**Варианты пуша**:

- **Standard**: обычный push
- **Setup**: push с установкой upstream
- **Force**: безопасный force push (только при необходимости)

#### 4.3 Проверка успешности операции

```bash
# Валидация пуша
git status                                   # Статус после пуша
git log --oneline origin/$target_branch -3   # Remote история
git remote show origin                       # Состояние remote
```

---

### 🎛️ Этап 5: Контрольные Точки и Безопасность

#### 5.1 Возможность отмены на каждом этапе

```bash
# Rollback функции
git reset --soft HEAD~1                      # Отменить коммит
git reset --hard HEAD~1                      # Отменить коммит + изменения
git checkout -- .                            # Отменить изменения в файлах
git clean -fd                                # Удалить новые файлы
```

**Точки отката**:

- **После анализа**: отмена всех операций
- **После staging**: отмена git add
- **После коммита**: git reset --soft/hard
- **После пуша**: git revert (создание обратного коммита)

#### 5.2 Логирование всех действий

```bash
# Логирование операций
echo "$(date): Старт автоматического коммита" >> git-automation.log
echo "$(date): Измененные файлы: $(git diff --name-only)" >> git-automation.log
echo "$(date): Сообщение коммита: $commit_message" >> git-automation.log
```

**Уровни логирования**:

- **INFO**: основные операции
- **DEBUG**: детальная информация
- **ERROR**: проблемы и их решения

#### 5.3 Оповещение о завершении процедуры

```bash
# Уведомления
echo "✅ Автоматический коммит завершен успешно"
echo "📊 Изменено файлов: $(git diff --name-only | wc -l)"
echo "🔗 Ветка: $(git branch --show-current)"
echo "🌐 Remote: $(git remote get-url origin)"
```

**Каналы оповещения**:

- **Console**: вывод в терминал
- **Log**: запись в файл лога
- **Notification**: системные уведомления (опционально)

---

## 🛠️ Необходимые Инструменты и Скрипты

### 📦 Основные инструменты

#### 1. **Git Automation Script** (`scripts/git-auto-commit.sh`)

- Основной скрипт автоматизации
- Интеграция всех этапов
- Обработка ошибок и rollback

#### 2. **Commit Message Generator** (`scripts/commit-message-generator.sh`)

- Анализ изменений
- Формирование conventional commits
- Интеграция с AI-ассистентами

#### 3. **Pre-flight Validator** (`scripts/pre-commit-validator.sh`)

- Расширенная валидация
- Проверка качества кода
- Интеграция с CI/CD

#### 4. **Git Operations Wrapper** (`scripts/git-ops-wrapper.sh`)

- Безопасные git операции
- Rollback функции
- Логирование

### 🔧 Дополнительные инструменты

#### **Зависимости**

```bash
# Базовые инструменты
git (>=2.30.0)
jq (JSON processing)
yq (YAML processing)

# Инструменты качества
pre-commit
shellcheck
markdownlint-cli
yamllint

# Опциональные уведомления
terminal-notifier (macOS)
notify-send (Linux)
```

#### **Конфигурационные файлы**

```yaml
# .git-auto-commit.yml
automation:
  default_branch: main
  auto_push: true
  pre_commit_check: true

commit_message:
  format: conventional
  max_length: 72
  include_scope: true

validation:
  run_pre_commit: true
  check_large_files: true
  max_file_size: 1MB

logging:
  level: INFO
  file: .git/logs/automation.log
  rotate: weekly
```

---

## 🎯 Implementation Priority

### 🔥 Phase 1 (Critical)

1. **Basic auto-commit script** - минимальная функциональность
2. **Commit message generator** - умные сообщения
3. **Safety checks** - rollback и валидация

### ⚡ Phase 2 (Important)

4. **Advanced validation** - интеграция с pre-commit
5. **Logging system** - детальное логирование
6. **Configuration** - гибкие настройки

### 🚀 Phase 3 (Nice to have)

7. **Notifications** - оповещения
8. **Integration** - с AI-ассистентами
9. **Dashboard** - веб-интерфейс (опционально)

---

## ✅ Готовность к Реализации

**🎯 Рекомендация**: Начать с Phase 1, создав базовый автоматизированный workflow с интеграцией существующих инструментов (`aic`, `cic`, `version-manager.sh`).

**🔑 Ключевое преимущество**: Проект уже имеет хорошую основу для автоматизации - нужно только создать единый workflow-менеджер.

**⚠️ Важно**: Обеспечить обратную совместимость с существующими командами `aic` и `cic`.
